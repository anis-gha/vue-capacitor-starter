import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'vue.capacitor.starter',
  appName: 'vue-capacitor-starter',
  webDir: 'dist',
}

export default config
